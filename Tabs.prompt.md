Switches between peer views — portal sections, program tiers, lab panels.

```jsx
<Tabs tabs={['Overview', 'Labs', 'Protocol']} value={tab} onChange={setTab} />
<Tabs variant="segmented" tabs={['Monthly', 'Quarterly']} value={cycle} onChange={setCycle} />
```

The active underline is gold — the only gold indicator in navigation.
