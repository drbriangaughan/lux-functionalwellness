Icon-only square button for toolbars, menus and close affordances.

```jsx
<IconButton label="Close" variant="ghost"><i data-lucide="x"></i></IconButton>
```

Sizes 32 / 40 / 48 — use `md` or larger for touch. Icons are Lucide at 1.75px stroke in a 20px box.
