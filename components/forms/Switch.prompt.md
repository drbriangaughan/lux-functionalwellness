Use for settings that apply instantly (notifications, sharing). For form data that needs a submit, use Checkbox.

```jsx
<Switch label="Share labs with my care team" checked={on} onChange={setOn} />
```
